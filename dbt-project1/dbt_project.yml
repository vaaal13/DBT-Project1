name: dbt_project1

profile: dbt_project1

model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_project1:
    +static_analysis: strict
    # Materialize staging models as views, and marts as tables
    bronze:
      +materialized: table